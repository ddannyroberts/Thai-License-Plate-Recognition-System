# 🔤 Character Segmentation System

## ✅ การเปลี่ยนแปลงที่ทำแล้ว

ระบบได้เพิ่ม **Character Segmentation** แล้ว! ตอนนี้เมื่อตรวจจับป้ายทะเบียนได้ จะ**แยกตัวอักษรทีละตัวก่อน** แล้วค่อย OCR แต่ละตัวอักษร

---

## 🎯 หลักการทำงาน

### **Pipeline ใหม่:**

```
1. Detector Model
   ↓ (ตรวจจับตำแหน่งป้ายทะเบียน)
   
2. Crop & Padding
   ↓ (ตัดส่วนป้ายทะเบียน)
   
3. Reader Model
   ↓ (ตรวจจับตัวอักษรแต่ละตัวบนป้าย)
   
4. Character Segmentation ✨ NEW
   ↓ (แยกตัวอักษรทีละตัว)
   
5. จัดเรียงตัวอักษร
   ↓ (ซ้ายไปขวา, บนลงล่าง)
   
6. OCR แต่ละตัวอักษร
   ↓ (อ่านทีละตัว)
   
7. รวมเป็นข้อความ
   ↓ (รวมตัวอักษรเป็นข้อความ)
   
8. Province Parser
   ↓ (แยกจังหวัด)
```

---

## 🔧 Technical Details

### **ไฟล์ใหม่: `api/character_segmentation.py`**

#### **Functions:**

1. **`read_plate_by_characters(plate_img)`**
   - ฟังก์ชันหลักที่ทำ Character Segmentation
   - Return: `(plate_text, character_details)`

2. **`extract_character_regions(plate_img, reader_predictions)`**
   - Extract character bounding boxes จาก Reader Model predictions
   - Filter ด้วย confidence threshold
   - Return: List of character regions

3. **`sort_characters_by_position(char_boxes)`**
   - จัดเรียงตัวอักษรตามตำแหน่ง (ซ้ายไปขวา, บนลงล่าง)
   - รองรับป้ายที่อาจมี 2 แถว

4. **`ocr_single_character(char_img, char_class)`**
   - OCR ตัวอักษรเดียว
   - ใช้ PSM 10 (single character mode)
   - Preprocessing หลายแบบเพื่อความแม่นยำ

---

## 🚀 การใช้งาน

### **วิธีทำงาน:**

1. **Reader Model** จะ detect ตัวอักษรแต่ละตัวบนป้าย (124 classes)
2. **Character Segmentation** จะ:
   - Extract bounding boxes ของแต่ละตัวอักษร
   - จัดเรียงตามตำแหน่ง
   - Crop แต่ละตัวอักษรออกมา
3. **OCR แต่ละตัวอักษร**:
   - Preprocess แต่ละตัวอักษร (grayscale, threshold, sharpen, etc.)
   - ใช้ Tesseract OCR PSM 10 (single character)
   - Filter ด้วย whitelist
4. **รวมเป็นข้อความ**:
   - รวมตัวอักษรทั้งหมดเป็นข้อความ
   - Clean up และ normalize

### **Fallback:**

ถ้า Character Segmentation ไม่ได้ผล (เช่น Reader Model ไม่ detect ตัวอักษร):
- จะ fallback ไปใช้ **OCR แบบเดิม** (อ่านทั้งป้าย)
- ระบบจะยังทำงานได้ตามปกติ

---

## 📊 Character Details

ระบบจะบันทึกข้อมูลของแต่ละตัวอักษร:

```json
{
  "character_details": [
    {
      "index": 0,
      "character": "ก",
      "confidence": 0.95,
      "x": 50.5,
      "y": 30.2,
      "bbox": {
        "x1": 45,
        "y1": 25,
        "x2": 56,
        "y2": 35
      },
      "model_class": "ก"
    },
    ...
  ]
}
```

ข้อมูลนี้จะถูกบันทึกใน `detections_json` ของแต่ละ record

---

## ⚙️ Configuration

### **Environment Variables:**

- `READER_MIN_CONF` - Confidence threshold สำหรับ character detection (default: 0.15)
- สามารถปรับได้ใน `character_segmentation.py`:
  - `min_conf` ใน `extract_character_regions()` - default: 0.3
  - `target_height` ใน `ocr_single_character()` - default: 100px

---

## 🔍 Debugging

ระบบจะแสดง debug logs:

```
DEBUG: Found 8 character detections
DEBUG: Extracted 8 character regions
DEBUG: Character segmentation result: 'กร 1234' from 8 characters
```

ถ้ามีปัญหา:
```
DEBUG: Character segmentation failed, falling back to full OCR
DEBUG Fallback OCR result: กร 1234
```

---

## ✅ ข้อดีของ Character Segmentation

1. **ความแม่นยำสูงขึ้น**: OCR แต่ละตัวอักษรแยกกันทำให้อ่านได้แม่นยำกว่า
2. **ลด Noise**: แต่ละตัวอักษรจะ preprocess แยกกัน
3. **รองรับหลายแถว**: จัดเรียงตัวอักษรได้ทั้งแถวเดียวและหลายแถว
4. **ใช้ Dataset**: ใช้ Reader Model ที่เทรนจาก dataset 124 classes

---

## 🐛 Troubleshooting

### **ปัญหาที่ 1: Character Segmentation ไม่ detect ตัวอักษร**

**สาเหตุ:**
- Reader Model ไม่ detect ตัวอักษร (confidence ต่ำ)
- ป้ายทะเบียนมีคุณภาพไม่ดี

**วิธีแก้:**
- ตรวจสอบ Reader Model predictions
- ลด `min_conf` threshold
- ระบบจะ fallback ไปใช้ OCR แบบเดิมอัตโนมัติ

### **ปัญหาที่ 2: ตัวอักษรเรียงผิด**

**สาเหตุ:**
- จัดเรียงตำแหน่งไม่ถูกต้อง

**วิธีแก้:**
- ตรวจสอบ `sort_characters_by_position()` function
- อาจต้องปรับ logic การจัดเรียง

### **ปัญหาที่ 3: OCR แต่ละตัวอักษรไม่แม่น**

**สาเหตุ:**
- ภาพตัวอักษรคุณภาพไม่ดี
- Preprocessing ไม่เหมาะสม

**วิธีแก้:**
- ปรับ preprocessing ใน `ocr_single_character()`
- เพิ่ม upscaling
- ปรับ threshold values

---

## 📝 Summary

ตอนนี้ระบบ:
- ✅ ใช้ **Character Segmentation** ก่อน OCR
- ✅ แยกตัวอักษรทีละตัวจาก Reader Model
- ✅ OCR แต่ละตัวอักษรแยกกัน
- ✅ รวมเป็นข้อความ
- ✅ มี **Fallback** ถ้า segmentation ไม่ได้ผล

**Happy Coding! 🚀**

